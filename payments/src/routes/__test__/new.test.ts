import request from 'supertest';
import { app } from '../../app';

it('returns a 404 when purchasing an order not exist', async() => {

});

it('returns a 401 when purchasing an order not belong the this user', async() => {

});

it('returns a 400 when purchasing an cancelled order', async() => {

});
